const { registerIssueToKanban, addCommentToIssue} = require('../../src/project/utils/issues')

describe('Open issue', ()=>{
  test('Add comment to issue', async () => {
    addCommentToIssue(, 'comment')
  })

  test('Add issue to Kanban', async () => {

  })
})